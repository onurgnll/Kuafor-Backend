const { tryCatchWrapper } = require("../helpers/tryCatchWrapper")

const cronForNotApprovedReservations = async () => {
    try {
        
        

    } catch (error) {
        console.log(error);
    }
}